language: python
python:
  - 3.6.1
services:
  - mongodb
  - mysql
before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS host_provider;'
  - make db_initialize
install:
  - pip install -r requirements_dev.txt
script:
  - make test
notifications:
  email: false