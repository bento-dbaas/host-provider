language: python
python:
  - 3.6.1
services:
  - mongodb
  - mysql
install:
  - pip install -r requirements_dev.txt
before_script:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS host_provider;'
  - make db_initialize
script:
  - make test
notifications:
  email: false